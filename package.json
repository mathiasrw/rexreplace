{
  "name": "rexreplace",
  "version": "2.1.0",
  "description": "Search and dynamicly replace text in files",
  "author": "Mathias Rangel Wulff",
  "license": "MIT",
  "main": "dist/multiversion.core.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/mathiasrw/rexreplace/"
  },
  "bin": {
    "rexreplace": "build/multiversion.cli.js",
    "rr": "build/multiversion.cli.js"
  },
  "scripts": {
    "test": "npm run build && npm run test-js && npm run test-cli",
    "prebuild": "rm -fr build && mkdir build build/ES6 && cp src/multiversion.core.js build/multiversion.core.js && cp src/multiversion.cli.js build/multiversion.cli.js && node -e \"file='src/core.js';fs = require('fs');fs.writeFileSync(file,fs.readFileSync(file,'utf8').replace(/(const version = \\')[^\\']+/m,'\\$1'+require('./package.json').version))\"",
    "build": "browserify --node src/cli.js -o build/ES6/rexreplace.cli.bundle.js && browserify --node src/core.js  -o build/ES6/rexreplace.core.bundle.js",
    "postbuild": "babel build/ES6 -d build/ES5",
    "test-js": "mocha",
    "test-cli": "npm -g install ./ && bash test/cli/run.sh",
    "test-speed": "bash test/speed/run.sh"
  },
  "keywords": [
    "replace",
    "regex",
    "sed"
  ],
  "devDependencies": {
    "assert": "^1.4.1",
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "browserify": "^14.1.0",
    "mocha": "^3.2.0"
  },
  "directories": {
    "test": "test/regression/js"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "globs": "^0.1.3",
    "yargs": "^7"
  }
}
